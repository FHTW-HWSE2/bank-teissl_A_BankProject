#include "logic/controller.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * @brief Entry point of the bank management system.
 * 
 * Initializes the application and starts the main menu loop.
 * 
 * @return int Exit status.
 */
int main()
{
    start();
    return 0;
}