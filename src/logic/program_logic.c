void start_program()
{
    user_decision = display_menu();
    user_decision = display_menu();
    switch()
    case 1: 
    create_account();
}

int create_account()
{
    calloc(account;)
    get_user_account_data(*account);
    validieren_daten();
    if(account ==ok)        
        save_account_to_csv();
    else
        get_corrupted_data<89;
}